#include <stdio.h>

/**
 * main - prints number of arguments passed into it
 * @argc: number of argument
 * @argv: arguments
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
